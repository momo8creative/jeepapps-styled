import { css } from "styled-components";

export const v = {
  sideBarWidth: "20em",
  smWith: "425px",
  mdWith: "768px",
  lgWith: "1024px",
};

export const btnReset = css`
  background: none;
  outline: none;
  border: none;
  color: inherit;
  text-align: inherit;
  font-size: inherit;
  padding: 0;
`;
